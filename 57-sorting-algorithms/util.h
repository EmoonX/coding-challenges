#pragma once

/** Shows program usage and aborts execution. */
void show_usage_and_exit();

/**
 * Swaps given values if x > y.
 * Returns whether swap happened or not.
 */
bool check_and_swap(int &x, int &y);
